/**
 * PostCSS configuration for Tailwind CSS.
 *
 * Tailwind CSS and Autoprefixer are used to process the CSS. No
 * additional plugins are configured at this time.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};